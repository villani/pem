// ENTRADA DE DADOS: 2 inteiros
// - à moda URI
var input = prompt("Entradas:");
var lines = input.split(" ");

var a = Number(lines[0]);
var b = Number(lines[1]);

// PROCESSAMENTO
var x = a + b;

// SAÍDA DE DADOS
console.log("X = " + x);